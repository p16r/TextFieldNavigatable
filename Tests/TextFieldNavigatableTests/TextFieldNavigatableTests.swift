import XCTest
@testable import TextFieldNavigatable

final class TextFieldNavigatableTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TextFieldNavigatable().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
